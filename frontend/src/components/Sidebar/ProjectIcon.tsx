const ProjectIcon = ({ letter }: { letter: string }) => (
  <div className="w-5 h-5 bg-white border border-gray-200 rounded text-[10px] font-bold text-gray-600 flex items-center justify-center shadow-sm">
    {letter}
  </div>
);

export default ProjectIcon;
